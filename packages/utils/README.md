# @df-persistence/utils v1.0.0

  所有包的一些通用方法
  

## Installation

  Using npm:

  ```shell
  $ npm i --save @df-persistence/utils
  ```

  ```js
    import { RegisterOption,GetLocationOption,LocationTypes,loadScript,load } from '@df-persistence/utils';
  ```

 

